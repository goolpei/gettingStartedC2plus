#include <iostream>
using namespace std;
int main(){
    int length, width, area;
    cout << "Enter length and width: ";
    cin >> length >> width;
    area = length * width;
    cout << "Area: " << area;
    return 0;
}